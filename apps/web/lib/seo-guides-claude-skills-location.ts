import type { SeoGuide } from "./seo-guides";

/**
 * Batch-1 guide slot for /guides/where-are-claude-skills-stored (SEO & AEO Desk brief, 2026-09-25).
 * Each batch-1 guide lives in its own file so the five guide PRs never touch the same lines.
 */
export const CLAUDE_SKILLS_LOCATION_GUIDES: SeoGuide[] = [];
