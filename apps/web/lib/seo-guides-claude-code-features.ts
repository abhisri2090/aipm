import type { SeoGuide } from "./seo-guides";

/**
 * Batch-1 guide slot for /guides/claude-code-skills-vs-mcp-vs-subagents-vs-hooks (SEO & AEO Desk brief, 2026-09-25).
 * Each batch-1 guide lives in its own file so the five guide PRs never touch the same lines.
 */
export const CLAUDE_CODE_FEATURE_COMPARISON_GUIDES: SeoGuide[] = [];
