import type { SeoGuide } from "./seo-guides";

/**
 * Batch-1 guide slot for /guides/share-claude-skills-with-team (SEO & AEO Desk brief, 2026-09-25).
 * Each batch-1 guide lives in its own file so the five guide PRs never touch the same lines.
 */
export const SHARE_CLAUDE_SKILLS_TEAM_GUIDES: SeoGuide[] = [];
