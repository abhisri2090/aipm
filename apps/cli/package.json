{
  "name": "@aipm/cli",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "aipm": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/bin.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aipm/engine": "workspace:*",
    "@aipm/schemas": "workspace:*",
    "commander": "^12.1.0"
  }
}
